# Place built Android .so files here before packaging:
#
#   arm64-v8a/libchroma_engine.so
#   armeabi-v7a/libchroma_engine.so
#   x86_64/libchroma_engine.so
#
# Run from repo root: ./tools/build_mobile.sh android
